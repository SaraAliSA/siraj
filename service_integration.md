# توثيق تكامل الخدمات والـ APIs في الواجهة الأمامية (Service Integration)

يوضح هذا الملف جميع نقاط الاتصال (API Endpoints) التي تتكامل معها الواجهة الأمامية (React + Vite) في تطبيق **سراج** وكيفية تداول البيانات بينها وبين الخادم الخلفي (FastAPI).

---

## 🔑 آلية المصادقة وحماية الطلبات (Authentication)
تعتمد الواجهة الأمامية على **Axios Client** في الملف `frontend/src/api/client.js` لإرسال جميع الطلبات مع الميزات التالية:
1. **حقن رمز الحماية (JWT Token Interceptor)**: يتم التحقق من وجود رمز `siraj_token` في الـ `localStorage` وإرفاقه تلقائياً في ترويسة الطلب كـ `Authorization: Bearer <Token>`.
2. **التعامل مع انتهاء الجلسة (401 Unauthorized)**: في حال إرجاع الخادم لرمز الحظر `401` (مثل انتهاء صلاحية الرمز)، يتم مسح الرمز وتوجيه المستخدم تلقائياً لصفحة تسجيل الدخول `/login`.

---

## 📊 جدول تكامل الخدمات والـ APIs

يسرد الجدول التالي كل ملف في الفرونت اند والخدمات المرتبطة به مع نقاط النهاية في الباك اند:

| ملف الفرونت اند (Component/Page) | الوظيفة البرمجية (Service) | طريقة الطلب (Method) | مسار الخدمة (API Endpoint) | نوع البيانات المرسلة / المستلمة |
| :--- | :--- | :--- | :--- | :--- |
| **`AuthContext.jsx`** | تسجيل مستخدم جديد | `POST` | `/auth/register` | إرسال: البريد، الاسم، كلمة المرور، العملة |
| | تسجيل الدخول للتحقق | `POST` | `/auth/login` | إرسال: البريد، كلمة المرور <br> استلام: رمز JWT وبيانات المستخدم |
| | استرجاع بيانات الحساب الحالي | `GET` | `/auth/me` | استلام: الملف الشخصي للمستخدم |
| **`AlertContext.jsx`** | جلب جميع التنبيهات | `GET` | `/alerts/` | استلام: مصفوفة بجميع التنبيهات |
| | جلب عدد التنبيهات غير المقروءة | `GET` | `/alerts/unread-count` | استلام: عدد التنبيهات النشطة الرقمي |
| | وضع علامة "مقروء" على التنبيه | `PUT` | `/alerts/{id}/read` | تحديث حالة التنبيه بالمعرف (UUID) |
| **`DashboardPage.jsx`** | جلب ملخص المداخيل والمصاريف | `GET` | `/dashboard/overview` | استلام: مجاميع الدخل والمصروف والادخار ومعدله |
| | جلب توزيع المصاريف للفئات | `GET` | `/dashboard/category-breakdown` | استلام: مصفوفة الفئات مع المبالغ والنسب المئوية |
| | جلب مؤشر الصحة المالي | `GET` | `/dashboard/health-score` | استلام: درجة الصحة (0-100) والتقدير اللفظي |
| | جلب نصيحة الذكاء الاصطناعي اليومية | `GET` | `/dashboard/daily-tip` | استلام: نصيحة مالية مخصصة بالعامية السعودية |
| | جلب آخر 5 معاملات | `GET` | `/transactions/` | استلام: مصفوفة المعاملات الأخيرة |
| | إضافة معاملة سريعة | `POST` | `/transactions/` | إرسال: المبلغ، الفئة، الوصف، التاريخ، النوع |
| **`TransactionsPage.jsx`** | جلب المعاملات مع فلاتر البحث | `GET` | `/transactions/` | معلمات البحث (Query): `type`, `category`, `start_date`, `end_date` |
| | حذف معاملة مالية | `DELETE` | `/transactions/{id}` | حذف المعاملة بالمعرف (UUID) |
| **`SirajAIPage.jsx`** | جلب قائمة جلسات الاستشارة | `GET` | `/chat/sessions` | استلام: مصفوفة جلسات الدردشة السابقة |
| | إنشاء جلسة استشارية جديدة | `POST` | `/chat/sessions` | إرسال: عنوان الجلسة (مثال: محادثة جديدة) |
| | جلب تاريخ رسائل جلسة معينة | `GET` | `/chat/sessions/{id}/messages` | استلام: سجل الرسائل بالترتيب الزمني للجلسة |
| | إرسال رسالة واستلام رد بث حي (SSE) | `POST` | `/chat/sessions/{id}/messages` | إرسال: نص الرسالة <br> استلام: بث حي للكلمات (Word-by-word Stream) |
| **`FinancingPage.jsx`** | استعراض المنتجات التمويلية | `GET` | `/financing/products` | استلام: قائمة القروض ونسب الربح والحد الأقصى |
| | تقديم طلب تمويل جديد | `POST` | `/financing/requests` | إرسال: نوع التمويل، المبلغ، فترة السداد، الملاحظات |
| | استرجاع سجل الطلبات المقدمة | `GET` | `/financing/requests` | استلام: سجل الطلبات وحالتها (مقبول/مرفوض/قيد الدراسة) |
| **`InvestmentPage.jsx`** | استعراض الفرص الاستثمارية | `GET` | `/investment/opportunities` | استلام: الصكوك والصناديق المتاحة مع العوائد المتوقعة |
| | تقديم طلب اشتراك استثماري | `POST` | `/investment/requests` | إرسال: اسم الفرصة، المبلغ، مستوى المخاطر، العائد المتوقع |
| | جلب استثماراتي النشطة | `GET` | `/investment/requests` | استلام: محفظة العميل وقيمة الاستثمارات الحالية |
| | جلب توصيات استثمارية ذكية | `GET` | `/investment/recommendations` | استلام: توصيات مخصصة مدعومة بالذكاء الاصطناعي مع الأسباب |
| **`SavingsPage.jsx`** | جلب الخطط الادخارية النشطة | `GET` | `/savings/plans` | استلام: قائمة أهداف الادخار مع نسبة التقدم |
| | إنشاء خطة ادخارية جديدة | `POST` | `/savings/plans` | إرسال: اسم الهدف، المبلغ الكلي، المدخر الحالي، الدفعة الشهرية، التاريخ المستهدف |
| | تحديث رصيد الادخار الحالي للخطة | `PUT` | `/savings/plans/{id}` | إرسال: المبلغ الجديد وحالة الخطة |
| | جلب تقديرات وتنبؤات الإنجاز | `GET` | `/savings/plans/{id}/progress` | استلام: الشهور المتبقية، تاريخ الإنجاز، ومؤشر الالتزام بالخطة |
| **`GoalsPage.jsx`** | جلب الأهداف المجدولة | `GET` | `/goals/` | استلام: الأهداف الشخصية الحالية للعميل |
| | جلب القوالب الجاهزة للأهداف | `GET` | `/goals/templates` | استلام: قوالب الأهداف الموسمية كالحج والعمرة والمدارس |
| | إنشاء هدف مالي جديد | `POST` | `/goals/` | إرسال: نوع الهدف، العنوان، المبلغ المستهدف، المتوفر حالياً، التاريخ |
| | توليد خطة مالية بالذكاء الاصطناعي | `POST` | `/goals/{id}/plan` | استلام: خطة شاملة تحتوي الدفعة المطلوبة ونصائح ترشيد الميزانية |
| **`AlertsPage.jsx`** | إعداد حد وتنبيه مخصص | `POST` | `/alerts/` | إرسال: نوع التنبيه، الفئة، الحد المالي، الرسالة التنبيهية |

---

## 📡 تفاصيل البث المباشر (SSE Stream) في المحادثة الذكية
تتميز صفحة **مساعد سراج الذكي** بالتعامل مع خدمات البث المباشر من خلال بروتوكول **Server-Sent Events** عبر الطلب `POST` للمسار `/chat/sessions/{id}/messages`.

*   **آلية المعالجة**: بدلاً من انتظار الرد الكامل، تقوم الواجهة الأمامية بفتح قارئ التدفق `ReadableStreamReader` وفك تشفير البايتات المستلمة بـ `TextDecoder` لتحديث شاشة الدردشة كلمة بكلمة أمام المستخدم بشكل حي وتفاعلي فائق السرعة.
